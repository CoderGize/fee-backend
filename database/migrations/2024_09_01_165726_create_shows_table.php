<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     *
     * @return void
     */
    public function up()
    {
        Schema::create('shows', function (Blueprint $table) {
            $table->id();
            $table->boolean('carousel_sh')->default(1);
            $table->boolean('testimonial_sh')->default(1);
            $table->boolean('designer_destimonial_sh')->default(1);
            $table->boolean('blog_sh')->default(1);
            $table->boolean('showroom_sh')->default(1);
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        Schema::dropIfExists('shows');
    }
};
